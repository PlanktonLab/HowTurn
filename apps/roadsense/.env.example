# Copy this file to .env.local and fill in your own Mapbox public access token.
# Get one at https://account.mapbox.com/access-tokens/ (the default public token,
# starting with "pk.", is fine -- this is a client-side token by design).
VITE_MAPBOX_TOKEN=
